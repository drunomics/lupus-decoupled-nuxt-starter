{
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "init-project-files": "nuxt-drupal-ce-init",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "start": "node .output/server/index.mjs",
    "postinstall": "nuxt prepare && npm run init-project-files",
    "lint": "eslint .",
    "lint-fix": "eslint . --fix"
  },
  "devDependencies": {
    "@nuxt/eslint": "^1.15.2",
    "nuxt": "^4.4.2",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "nuxtjs-drupal-ce": "^2.6.0"
  }
}
